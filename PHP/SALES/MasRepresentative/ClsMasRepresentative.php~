<?php
    require($_SERVER["DOCUMENT_ROOT"]."/dbConn.php");



    $task='loadRepresentative';

    if ( isset($_POST['task'])){
        $task = $_POST['task']; // Get this from Ext
    }
        mysqli_set_charset($conn, "utf8");
    switch($task){
		case "loadRepresentative":
		getRepresentative();
		break;

		case "loadRepGroup":
		getRepGroup();
		break;
	        default:
        	    echo "{failure:true}";  // Simple 1-dim JSON array to tell Ext the request failed.
        	    break;
    }
    
    function JEncode($arr){
        $data = json_encode($arr, JSON_UNESCAPED_UNICODE);    //encode the data in json format
        return $data;
    }
    
   
 function getRepresentative()
    {
        mysqli_set_charset($conn, "utf8");

        $r=mysql_query("select * from massal_repr , acc_group_master where repr_accgrp = grp_code");
	$nrow = mysqli_num_rows($r);
	while($re = mysqli_fetch_array($r))
	{
	$arr[]= $re ;
        }
		$jsonresult = JEncode($arr);
		echo '({"total":"'.$nrow.'","results":'.$jsonresult.'})';
    }
	
 function getRepGroup()
    {
        mysqli_set_charset($conn, "utf8");

        $r=mysql_query(" select * from acc_group_master where grp_parent_code = 24 order by grp_name");
	$nrow = mysqli_num_rows($r);
	while($re = mysqli_fetch_array($r))
	{
	$arr[]= $re ;
        }
		$jsonresult = JEncode($arr);
		echo '({"total":"'.$nrow.'","results":'.$jsonresult.'})';
    }
	
?>
