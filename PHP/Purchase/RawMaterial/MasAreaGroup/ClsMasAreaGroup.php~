<?php
    require($_SERVER["DOCUMENT_ROOT"]."/dbConn.php");

    $task='loadLotList';

    if ( isset($_POST['task'])){
        $task = $_POST['task']; // Get this from Ext
    }
        mysqli_set_charset($conn, "utf8");
    switch($task){
		case "loadAreaList":
		getAreaList();
		break;

		case "loadAreaRepGroupList":
	        getAreaRepGroupList();
		break;




	        default:
        	    echo "{failure:true}";  // Simple 1-dim JSON array to tell Ext the request failed.
        	    break;
    }
    
    function JEncode($arr){
        $data = json_encode($arr, JSON_UNESCAPED_UNICODE);    //encode the data in json format
        return $data;
    }
    
   
 function getAreaList()
    {
        mysqli_set_charset($conn, "utf8");
        $r=mysql_query("select areagrp_code, areagrp_name from mas_areagroup order by areagrp_name asc");
	$nrow = mysqli_num_rows($r);
	while($re = mysqli_fetch_array($r))
	{
	$arr[]= $re ;
        }
		$jsonresult = JEncode($arr);
		echo '({"total":"'.$nrow.'","results":'.$jsonresult.'})';
    }
	
 function getAreaRepGroupList()
    {
        mysqli_set_charset($conn, "utf8");
        $r=mysql_query("select mas_area_repgrp_code, mas_area_repgrp_name from mas_area_report_group order by mas_area_repgrp_name asc");
	$nrow = mysqli_num_rows($r);
	while($re = mysqli_fetch_array($r))
	{
	$arr[]= $re ;
        }
		$jsonresult = JEncode($arr);
		echo '({"total":"'.$nrow.'","results":'.$jsonresult.'})';
    }
	
	
?>
